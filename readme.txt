readme for project 1 for CompEN 462

To run the matlab file, press the run button on top at the matlab IDE.

After the program executes, type "final_string_output" on the command window to get the final string that the program generates after execution. 
The final string we get is :
"A convolUtion in the time dom`in is!multipdicat)on mn the freauency domain."